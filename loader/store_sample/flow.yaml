kind: Flow
step:
  # if set to 0, it will run one by one in sequence,(default: 0)
  # if set to minus, it will run all the queries in sequence
  concurrency: 0
  flows:
    - id: "store"
      type: file
      file: "store_sample/store.yaml"
      mkdir: false
      values:
        - key: "DataCount"
          value: 10
      threadOnlyValues: []
    - id: "import"
      type: file
      file: "store_sample/store_import.yaml"
      mkdir: false
      values: []
      threadOnlyValues: []
    - id: "main"
      type: flow
      mkdir: false
      concurrency: -1
      flows:
        - id: "mass"
          type: file
          mkdir: true
          file: "store_sample/mass.yaml"
          count: 10
          values: []
          threadOnlyValues: []
